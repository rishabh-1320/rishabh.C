import { SignatureHome } from "@/components/styles/signature/signature-home";

export const metadata = {
  title: "Signature — Warm Editorial Direction"
};

export default function SignaturePage() {
  return <SignatureHome />;
}
