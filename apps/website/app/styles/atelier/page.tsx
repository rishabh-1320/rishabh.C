import { AtelierHome } from "@/components/styles/atelier/atelier-home";

export const metadata = {
  title: "Editorial Atelier — Direction C"
};

export default function AtelierPage() {
  return <AtelierHome />;
}
