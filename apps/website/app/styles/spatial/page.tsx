import { SpatialHome } from "@/components/styles/spatial/spatial-home";

export const metadata = {
  title: "Cinematic Spatial — Direction B"
};

export default function SpatialPage() {
  return <SpatialHome />;
}
